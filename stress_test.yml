config:
  target: 'http://localhost:3000'
  phases:
    - duration: 10
      arrivalRate: 350
  defaults:
    headers:

scenarios:
  - flow:
    - get:
        url: "/"
    - get:
        url: "/data/toptracks"

